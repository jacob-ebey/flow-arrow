# higher-order-nodes library target
#
# Pure cdylib-friendly variant used to exercise static node parameters in
# export-oriented targets.
import std.fault { expect }
import std.math { add_i64 as add }

node increment(x: i64) -> y: i64 {
    ($x, 1) -> add -> expect -> $y
}

node twice<step: node(i64) -> i64>(x: i64) -> y: i64 {
    $x -> step -> step -> $y
}

extern node answer(seed: i64) -> value: i64 {
    $seed -> twice<increment> -> $value
}
